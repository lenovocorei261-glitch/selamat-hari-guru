<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Hari Guru | Terima Kasih Telah Menyalakan Dunia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <style>
    :root{
      --gold:#d4af37;
      --dark:#111;
      --overlay:rgba(0,0,0,.55);
    }
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%;font-family:'Poppins',sans-serif;color:#fff}

    /* Background foto full-screen */
    .hero{
      position:relative;
      width:100%;
      height:100vh;
      background:url("foto-anda.jpg") center/cover no-repeat fixed;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
    }
    .hero::before{ /* gelap tipis supaya teks keluar */
      content:"";
      position:absolute;
      inset:0;
      background:var(--overlay);
    }

    /* Konten */
    .content{
      position:relative;
      z-index:2;
      max-width:800px;
      padding:0 20px;
      animation:fadeUp 1.2s ease-out;
    }
    .content h1{
      font-family:'Playfair Display',serif;
      font-size:clamp(3rem,8vw,5.5rem);
      letter-spacing:2px;
      margin-bottom:.4em;
      color:var(--gold);
    }
    .content p{
      font-size:clamp(1.1rem,2.5vw,1.4rem);
      font-weight:300;
      line-height:1.7;
      margin-bottom:2em;
    }
    .btn{
      display:inline-block;
      padding:14px 36px;
      border:2px solid var(--gold);
      color:var(--gold);
      text-decoration:none;
      font-weight:600;
      letter-spacing:1px;
      transition:.3s;
    }
    .btn:hover{
      background:var(--gold);
      color:var(--dark);
    }

    /* Animasi */
    @keyframes fadeUp{
      from{opacity:0;transform:translateY(30px)}
      to{opacity:1;transform:translateY(0)}
    }

    /* Responsif kecil */
    @media(max-width:600px){
      .content h1{letter-spacing:1px}
      .btn{padding:12px 28px;font-size:.95rem}
    }
  </style>
<base target="_blank">
</head>
<body>

  <section class="hero">
    <div class="content">
      <h1>Selamat Hari Guru</h1>
      <p>
        “Guru adalah lilin yang membakar dirinya demi menerangi jalan bagi orang lain.”<br>
        Terima kasih telah menjadi cahaya dalam perjalanan kami.
      </p>
      <a href="#tribute" class="btn">Tuliskan Ucapan</a>
    </div>
  </section>

  <!-- Opsional: area ucapan -->
  <section id="tribute" style="background:#fafafa;color:#222;padding:80px 20px;text-align:center">
    <h2 style="font-family:'Playfair Display',serif;font-size:2.2rem;margin-bottom:.5em;color:var(--dark)">Tulis Ucapanmu</h2>
    <form action="https://formspree.io/f/your-id" method="POST" style="max-width:600px;margin:auto;display:grid;gap:15px">
      <input type="text" name="nama" placeholder="Nama Kamu" required style="padding:14px;font-size:1rem;border:1px solid #ccc;border-radius:4px">
      <textarea name="ucapan" rows="4" placeholder="Ucapan & terima kasihmu..." required style="padding:14px;font-size:1rem;border:1px solid #ccc;border-radius:4px;resize:vertical"></textarea>
      <button type="submit" style="padding:14px;background:var(--gold);color:#fff;border:none;font-size:1rem;font-weight:600;cursor:pointer;border-radius:4px;transition:.3s">Kirim Ucapan</button>
    </form>
  </section>

</body>
</html>
